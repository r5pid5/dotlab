import {layerBounds} from './renderer.js';
export function handles(layer,canvas,assets,displayWidth){const b=layerBounds(layer,canvas.width,canvas.height,assets),u=canvas.width/Math.max(1,displayWidth),pad=10*u,a=layer.rotation*Math.PI/180,local=(x,y)=>({x:b.x+x*Math.cos(a)-y*Math.sin(a),y:b.y+x*Math.sin(a)+y*Math.cos(a)}),hw=b.width/2+pad,hh=b.height/2+pad;return{...b,u,corners:[[-hw,-hh],[hw,-hh],[hw,hh],[-hw,hh]].map(([x,y])=>local(x,y)),rotate:local(0,-hh-30*u),top:local(0,-hh),remove:local(-hw-17*u,-hh-17*u)}}
export function paintHandles(ctx,g){ctx.save();ctx.strokeStyle='#d9ff5f';ctx.fillStyle='#20251b';ctx.lineWidth=1.3*g.u;ctx.setLineDash([5*g.u,4*g.u]);ctx.beginPath();g.corners.forEach((p,i)=>i?ctx.lineTo(p.x,p.y):ctx.moveTo(p.x,p.y));ctx.closePath();ctx.stroke();ctx.setLineDash([]);ctx.beginPath();ctx.moveTo(g.top.x,g.top.y);ctx.lineTo(g.rotate.x,g.rotate.y);ctx.stroke();for(const p of g.corners){ctx.beginPath();ctx.arc(p.x,p.y,5*g.u,0,Math.PI*2);ctx.fill();ctx.stroke()}ctx.beginPath();ctx.arc(g.rotate.x,g.rotate.y,9*g.u,0,Math.PI*2);ctx.fill();ctx.stroke();ctx.font=`${13*g.u}px Arial`;ctx.fillStyle='#d9ff5f';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText('↻',g.rotate.x,g.rotate.y);ctx.fillStyle='#28251f';ctx.beginPath();ctx.arc(g.remove.x,g.remove.y,9*g.u,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#ffb1ae';ctx.stroke();ctx.fillStyle='#ffb1ae';ctx.fillText('×',g.remove.x,g.remove.y);ctx.restore()}
export function hitHandle(g,x,y){const hit=p=>Math.hypot(x-p.x,y-p.y)<13*g.u;if(hit(g.remove))return'delete';if(hit(g.rotate))return'rotate';if(g.corners.some(hit))return'resize';return null}
export function zoomAt(s,canvas,x,y,newZoom,photoBox){if(!Number.isFinite(newZoom)||newZoom<=0)return;const box=photoBox(s,canvas.width,canvas.height),cx=(box.x+box.width/2)/canvas.width,cy=(box.y+box.height/2)/canvas.height,k=newZoom/s.zoom;s.panX=(x-cx-(x-cx-s.panX/100)*k)*100;s.panY=(y-cy-(y-cy-s.panY/100)*k)*100;s.zoom=newZoom;}
